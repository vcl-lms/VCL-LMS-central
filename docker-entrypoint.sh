#!/bin/bash

# Add any setup commands needed for your application
# For example, you may want to run database migrations, set environment variables, etc.

# Execute the CMD command from the Dockerfile
exec "$@"
